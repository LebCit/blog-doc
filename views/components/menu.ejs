<%# Menu toggle %>
<span id="menuLink" class="menu-link" tabindex="0">
	<%# Hamburger icon %>
	<span></span>
</span>

<%# Menu %>
<div id="menu">
	<div class="pure-menu">
		<a href="/" class="pure-menu-heading">BLOG-DOC</a>

		<% let links = {archive: "Archive" , tags: "Tags", search: "Search", doc: "Documentation", license: "License",
		"https://github.com/LebCit/blog-doc": "GitHub Repo"} %>

		<ul class="pure-menu-list">
			<% for (let link in links) { %><%#
			<!-- Get each menu link value -->
			%><% let linkValue = links[link] %><%#
			<!-- If a link refers to an external URL -->
			%><% if (link.startsWith('http')) { %>
			<li class="pure-menu-item">
				<a href="<%= link %>" class="pure-menu-link"><%= linkValue %></a>
			</li>
			<% } else if (locals.build || locals.dynamic) { %>
			<li class="pure-menu-item">
				<a href="/<%= link %>.html" class="pure-menu-link"><%= linkValue %></a>
			</li>
			<% } else { %>
			<li class="pure-menu-item">
				<a href="/<%= link %>" class="pure-menu-link"><%= linkValue %></a>
			</li>
			<% } %><%#
			<!-- End of for loop -->
			%><% } %>
		</ul>
	</div>
</div>
